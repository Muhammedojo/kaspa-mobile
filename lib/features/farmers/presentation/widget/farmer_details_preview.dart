import 'package:flutter/material.dart';

class FarmerConfirmation extends StatelessWidget {
  const FarmerConfirmation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}